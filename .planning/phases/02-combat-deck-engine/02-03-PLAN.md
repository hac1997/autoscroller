---
phase: 02-combat-deck-engine
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/scenes/CombatScene.ts
  - src/scenes/PostCombatScene.ts
  - src/scenes/RewardScene.ts
  - src/scenes/DeckViewScene.ts
  - src/scenes/ShopDeckEditor.ts
  - src/scenes/DeathScene.ts
  - src/ui/CardVisual.ts
  - src/ui/CardQueueDisplay.ts
  - src/ui/CombatHUD.ts
  - src/ui/SynergyFlash.ts
  - src/ui/DragDropDeckEditor.ts
autonomous: false
requirements:
  - CMBT-03
  - CMBT-04
  - CMBT-05
  - DECK-07
  - DECK-08
  - PLSH-01

must_haves:
  truths:
    - "Player can see the card queue scrolling during combat with the current card highlighted"
    - "Synergy triggers show COMBO! flash at screen center with synergy color and bonus text"
    - "Post-combat summary displays damage dealt, damage taken, cards played, combos, XP earned"
    - "Card reward screen shows 3 enlarged cards with rarity borders, player can pick 1 or skip"
    - "Deck viewer displays all cards in grid with synergy pair highlights"
    - "Shop deck editor supports drag-and-drop reorder and gold-cost card removal"
    - "Death screen shows run statistics: loops completed, total damage, total cards played, total combos, cause of death"
  artifacts:
    - path: "src/scenes/CombatScene.ts"
      provides: "Thin scene wrapper: creates CombatEngine, subscribes to events, renders via UI components"
      min_lines: 80
    - path: "src/scenes/PostCombatScene.ts"
      provides: "Summary overlay with stats grid and Continue button"
      min_lines: 40
    - path: "src/scenes/RewardScene.ts"
      provides: "Card reward choice with 3 cards, selection, Take/Skip buttons"
      min_lines: 60
    - path: "src/scenes/DeckViewScene.ts"
      provides: "Full deck grid with synergy highlights and card count"
      min_lines: 40
    - path: "src/scenes/ShopDeckEditor.ts"
      provides: "Drag-and-drop reorder + card removal with gold costs"
      min_lines: 80
    - path: "src/scenes/DeathScene.ts"
      provides: "Run statistics display with Return to Menu button"
      min_lines: 40
    - path: "src/ui/CardVisual.ts"
      provides: "Reusable card visual component (72x96 or 108x144 enlarged)"
    - path: "src/ui/CardQueueDisplay.ts"
      provides: "Vertical scrolling card queue for combat scene"
    - path: "src/ui/CombatHUD.ts"
      provides: "HP/Stamina/Mana bars + enemy info + cooldown arc"
    - path: "src/ui/SynergyFlash.ts"
      provides: "COMBO! text animation + card glow effect"
    - path: "src/ui/DragDropDeckEditor.ts"
      provides: "Draggable card list with drop indicators"
  key_links:
    - from: "src/scenes/CombatScene.ts"
      to: "src/systems/combat/CombatEngine.ts"
      via: "creates engine in create(), calls engine.tick(delta) in update()"
      pattern: "new CombatEngine|engine\\.tick\\("
    - from: "src/scenes/CombatScene.ts"
      to: "src/core/EventBus.ts"
      via: "subscribes to combat:card-played, combat:synergy-triggered, combat:end etc"
      pattern: "eventBus\\.on\\('combat:"
    - from: "src/scenes/PostCombatScene.ts"
      to: "src/systems/combat/CombatStats.ts"
      via: "reads CombatStats passed from CombatScene"
      pattern: "CombatStats|stats\\.damageDealt"
    - from: "src/scenes/RewardScene.ts"
      to: "src/systems/deck/LootSystem.ts"
      via: "calls generateCardReward for card choices"
      pattern: "generateCardReward"
    - from: "src/scenes/RewardScene.ts"
      to: "src/systems/deck/DeckSystem.ts"
      via: "calls addCard when player takes a card"
      pattern: "addCard\\("
    - from: "src/scenes/ShopDeckEditor.ts"
      to: "src/systems/deck/DeckSystem.ts"
      via: "calls removeCard, reorderDeck, getRemovalCost"
      pattern: "removeCard\\(|reorderDeck\\(|getRemovalCost\\("
    - from: "src/scenes/DeathScene.ts"
      to: "src/systems/hero/XPSystem.ts"
      via: "calls loseAllRunXP on death"
      pattern: "loseAllRunXP\\("
---

<objective>
Build all combat and deck management UI as Phaser scenes and reusable UI components, wiring them to the pure TS systems from Plans 01 and 02. Includes CombatScene (thin wrapper), post-combat summary, card reward choice, deck viewer, shop deck editor with drag-and-drop, and death screen.

Purpose: This is what the player sees and interacts with. The auto-combat card queue is the game's primary visual; the synergy flash is the dopamine hit; the deck editor is where strategy happens. All rendering delegates to the engine systems.

Output: 6 scene files, 5 UI component files, visual checkpoint for manual verification.
</objective>

<execution_context>
@C:/Users/nicolas.pereira/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nicolas.pereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-combat-deck-engine/02-CONTEXT.md
@.planning/phases/02-combat-deck-engine/02-UI-SPEC.md
@.planning/phases/02-combat-deck-engine/02-RESEARCH.md

<!-- Load Plan 01 and 02 summaries for interface contracts -->
@.planning/phases/02-combat-deck-engine/02-01-SUMMARY.md
@.planning/phases/02-combat-deck-engine/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs: -->
From src/systems/combat/CombatEngine.ts:
```typescript
export class CombatEngine {
  constructor(state: CombatState);
  tick(deltaMs: number): void;
  getStats(): CombatStats;
  getState(): CombatState;
  isComplete(): boolean;
}
```

From src/systems/combat/CombatState.ts:
```typescript
export interface CombatState {
  heroHP: number; heroMaxHP: number;
  heroStamina: number; heroMaxStamina: number;
  heroMana: number; heroMaxMana: number;
  heroDefense: number;
  heroStrength: number; heroDefenseMultiplier: number;
  heroClass: string;
  deckOrder: string[];
  enemyId: string; enemyName: string;
  enemyHP: number; enemyMaxHP: number;
  enemyDefense: number; enemyDamage: number;
  enemyAttackCooldown: number;
  enemyPattern: string; enemySpecialEffect: string | null;
  activePassives: any[];
}
export function createCombatState(run: RunState, enemy: ScaledEnemy): CombatState;
```

From src/systems/combat/CombatStats.ts:
```typescript
export interface CombatStats {
  damageDealt: number; damageReceived: number;
  cardsPlayed: number; synergiesTriggered: number;
  reshuffles: number; cardsSkipped: number;
  enemyId: string; enemyName: string;
  result: 'victory' | 'defeat' | 'ongoing';
}
```

<!-- From Plan 02 outputs: -->
From src/systems/deck/DeckSystem.ts:
```typescript
export function addCard(cardId: string, run: RunState): void;
export function removeCard(cardId: string, run: RunState): boolean;
export function reorderDeck(newOrder: string[], run: RunState): boolean;
export function getRemovalCost(run: RunState): number;
export const REORDER_SESSION_COST: number; // 30
```

From src/systems/deck/LootSystem.ts:
```typescript
export function generateCardReward(rng: RNG, count?: number, excludeIds?: string[]): string[];
export function shouldOfferReward(enemyType: string, rng: RNG): boolean;
```

From src/systems/hero/XPSystem.ts:
```typescript
export function earnXP(run: RunState, amount: number): void;
export function loseAllRunXP(run: RunState): void;
export function getXPForEnemy(enemyType: string): number;
```

<!-- From Phase 1 outputs: -->
From src/core/EventBus.ts:
```typescript
export const eventBus: EventBus;
// Events: combat:card-played, combat:synergy-triggered, combat:card-skipped, combat:deck-reshuffled, combat:enemy-attack, combat:end
```

From src/state/RunState.ts:
```typescript
export function getRun(): RunState;
```

From src/data/DataLoader.ts:
```typescript
export function getCardById(id: string): CardDefinition | undefined;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable UI components + CombatScene thin wrapper + CombatHUD + CardQueue + SynergyFlash</name>
  <files>
    src/ui/CardVisual.ts,
    src/ui/CardQueueDisplay.ts,
    src/ui/CombatHUD.ts,
    src/ui/SynergyFlash.ts,
    src/scenes/CombatScene.ts
  </files>
  <read_first>
    src/systems/combat/CombatEngine.ts,
    src/systems/combat/CombatState.ts,
    src/systems/combat/CombatStats.ts,
    src/core/EventBus.ts,
    src/state/RunState.ts,
    src/data/DataLoader.ts,
    src/data/types.ts,
    src/effects/CombatEffects.ts,
    .planning/phases/02-combat-deck-engine/02-UI-SPEC.md
  </read_first>
  <action>
    1. **Create `src/ui/CardVisual.ts`** -- reusable Phaser Container for card display:
       - Factory function: `createCardVisual(scene: Phaser.Scene, x: number, y: number, cardId: string, options?: { enlarged?: boolean }): Phaser.GameObjects.Container`
       - Standard size: 72x96px. Enlarged: 108x144px (1.5x scale for reward screen)
       - Background: Rectangle at `0x222222`, stroke 2px in rarity color (`0xcccccc` common, `0x33cc33` uncommon, `0xff6600` rare)
       - Type indicator: 4px tall colored strip at top (attack=`0xcc3333`, defense=`0x3366cc`, spell/magic=`0x9933cc`)
       - Card name: 16px white centered, word-wrap at width-8px
       - Cost indicator: 14px bottom-left, stamina color `0xff8c00` or mana color `0x6a5acd` based on card category
       - Cooldown: 14px bottom-right, muted `#aaaaaa`, e.g. "1.2s"
       - Hover state: `setInteractive()`, on pointerover scale to 1.1 with 200ms ease, stroke brightens to white; on pointerout restore

    2. **Create `src/ui/CardQueueDisplay.ts`** -- vertical card queue for combat scene:
       - Class `CardQueueDisplay` with constructor `(scene: Phaser.Scene)`
       - Positioned right-aligned at x=728 (800-72)
       - Shows 5 cards at a time using `createCardVisual` at standard size
       - 8px gap between cards vertically (5 * 96 + 4 * 8 = 512px)
       - Current card (index 0): full opacity, accent pulse glow (tween scale 1.0->1.05 loop 0.8s, stroke `0xffd700`)
       - Next cards (index 1-4): alpha 0.7
       - Triangle arrow indicator left of current card
       - `onCardPlayed(deckPosition: number)`: top card slides left (x-=100) and fades out (300ms), remaining cards slide up (200ms), new card appears at bottom
       - `onCardSkipped(deckPosition: number)`: flash "Skipped" muted text on card for 600ms
       - `onDeckReshuffled()`: brief flash "Deck Reset" label (14px muted) at queue bottom for 800ms
       - `update(state: CombatState, deckPointer: number)`: refresh display from current state
       - `destroy()`: clean up all game objects and tweens

    3. **Create `src/ui/CombatHUD.ts`** -- HP/stamina/mana bars + enemy info + cooldown arc:
       - Class `CombatHUD` with constructor `(scene: Phaser.Scene)`
       - Container at y=8, setScrollFactor(0), setDepth(100)
       - **Hero section (left, x=8):**
         - HP bar: 160x12px, fill color changes by HP%: `0x00ff00` >50%, `0xffaa00` 25-50%, `0xff0000` <25%
         - HP text: 14px "{current}/{max}" right of bar
         - Stamina bar: 120x8px, fill `0xff8c00`
         - Mana bar: 120x8px, fill `0x6a5acd`
         - Labels at 14px, 4px vertical gap between bars
       - **Enemy section (right, x=520):**
         - Enemy name: 16px white
         - HP bar: 160x12px, fill `0xff0000`
         - HP text: 14px
       - **Cooldown arc (center, x=400):**
         - Phaser Graphics circle arc, 24px radius, fill `0xffd700`
         - Center text: remaining seconds at 14px
         - Updates each frame from cooldown timer value
       - `update(state: CombatState, heroCooldown: number, heroMaxCooldown: number)`: redraw bars and cooldown arc
       - `destroy()`: cleanup

    4. **Create `src/ui/SynergyFlash.ts`** -- COMBO! text animation:
       - Function `showSynergyFlash(scene: Phaser.Scene, bonusType: string, bonusValue: number, displayName: string): void`
       - "COMBO!" text: 32px bold, color `0xff00ff`, at canvas center (400, 300)
       - Animation: scale from 0 to 1.3 over 200ms (Back.easeOut), hold 400ms, fade to alpha 0 over 300ms
       - Bonus text below: 16px, `#ff00ff`, shows displayName + " +" + bonusValue (e.g., "Counter Attack! +8 damage")
       - Bonus animation: fade in 100ms after COMBO, float up 30px and fade over 600ms
       - Auto-destroys all created objects after animation completes

    5. **Rewrite `src/scenes/CombatScene.ts`** as thin wrapper:
       - `create(data: { enemyId: string })`:
         - Read RunState via `getRun()`
         - Scale enemy via existing `scaleEnemy` + `getEnemyById`
         - Create `CombatState` via `createCombatState(run, scaledEnemy)`
         - Create `CombatEngine` with the state
         - Create `CombatHUD`, `CardQueueDisplay` UI components
         - Subscribe to EventBus events with named handlers:
           - `combat:card-played` -> `cardQueue.onCardPlayed(data.deckPosition)` + update HUD
           - `combat:synergy-triggered` -> `showSynergyFlash(scene, data.bonusType, data.bonusValue, data.displayName)` + glow cards in queue
           - `combat:card-skipped` -> `cardQueue.onCardSkipped(data.deckPosition)`
           - `combat:deck-reshuffled` -> `cardQueue.onDeckReshuffled()`
           - `combat:enemy-attack` -> show floating damage number via CombatEffects (reuse existing)
           - `combat:end` -> show "VICTORY" or "DEFEAT" display text (32px bold, accent or destructive color), hold 1s, then transition:
             - Victory: `earnXP(run, getXPForEnemy(enemyType))`, write HP back to RunState, transition to PostCombatScene with stats
             - Defeat: `loseAllRunXP(run)`, transition to DeathScene
       - `update(_time, delta)`: single line: `this.engine.tick(delta)`
       - `cleanup()` on `shutdown` event: unsubscribe all EventBus listeners, destroy HUD/CardQueue, destroyAll children
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/CardVisual.ts contains `export function createCardVisual(` with parameters `scene`, `x`, `y`, `cardId`
    - src/ui/CardVisual.ts contains `0xcc3333` (attack color) and `0x3366cc` (defense color) and `0x9933cc` (spell color)
    - src/ui/CardQueueDisplay.ts contains `export class CardQueueDisplay` with methods `onCardPlayed`, `onCardSkipped`, `onDeckReshuffled`, `update`, `destroy`
    - src/ui/CombatHUD.ts contains `export class CombatHUD` with `update(` method
    - src/ui/SynergyFlash.ts contains `export function showSynergyFlash(` and contains `COMBO!` string and `0xff00ff` color
    - src/scenes/CombatScene.ts contains `new CombatEngine(` and `engine.tick(delta)` in update method
    - src/scenes/CombatScene.ts contains `eventBus.on('combat:card-played'` and `eventBus.on('combat:synergy-triggered'` and `eventBus.on('combat:end'`
    - src/scenes/CombatScene.ts contains `eventBus.off(` in a cleanup method
    - TypeScript compiles with no errors (`npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>CombatScene is a thin wrapper that creates CombatEngine, delegates tick(), subscribes to EventBus events, and renders via CardQueueDisplay, CombatHUD, and SynergyFlash components -- compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: PostCombatScene + RewardScene + DeckViewScene + ShopDeckEditor + DeathScene</name>
  <files>
    src/scenes/PostCombatScene.ts,
    src/scenes/RewardScene.ts,
    src/scenes/DeckViewScene.ts,
    src/scenes/ShopDeckEditor.ts,
    src/scenes/DeathScene.ts,
    src/ui/DragDropDeckEditor.ts
  </files>
  <read_first>
    src/scenes/CombatScene.ts,
    src/ui/CardVisual.ts,
    src/systems/combat/CombatStats.ts,
    src/systems/deck/DeckSystem.ts,
    src/systems/deck/LootSystem.ts,
    src/systems/hero/XPSystem.ts,
    src/state/RunState.ts,
    src/data/DataLoader.ts,
    .planning/phases/02-combat-deck-engine/02-UI-SPEC.md
  </read_first>
  <action>
    1. **Create `src/scenes/PostCombatScene.ts`** -- overlay scene showing combat results:
       - Reads CombatStats from scene data: `this.scene.settings.data.stats as CombatStats`
       - Overlay panel: centered Rectangle 500x380 at `0x222222`, alpha 0.9
       - Title: "Battle Summary" at 24px bold, centered at top with 24px padding
       - Stats grid (two columns, left=label in muted `#aaaaaa` 16px, right=value in white 16px):
         - "Damage Dealt": value in white
         - "Damage Taken": value in `#ff0000` if > 0
         - "Cards Played": value in white
         - "Combos": value in `#ff00ff` if > 0
         - "XP Earned": value in `#00ccff`
       - 8px vertical gap between rows, 24px between stats and CTA
       - "Continue" button: 24px bold, `#ffd700`, centered at bottom. On click: check `shouldOfferReward()` -- if true, transition to RewardScene; if false, return to game scene
       - Cleanup on shutdown: destroy all children

    2. **Create `src/scenes/RewardScene.ts`** -- card reward choice:
       - Overlay panel: centered Rectangle 600x400 at `0x222222`, alpha 0.9
       - Title: "Choose a Card" at 24px bold, centered top
       - Instruction: "Pick one card to add to your deck, or skip." at 16px muted, below title
       - Generate 3 card IDs via `generateCardReward(rng, 3)`
       - Display 3 `createCardVisual` at enlarged size (108x144), horizontally centered with 64px gap, at y=200
       - Each card interactive: on click, selected card gets accent stroke (3px `0xffd700`, scale 1.05), unselected dim to alpha 0.5
       - Clicking different card switches selection
       - "Take Card" button: 24px bold, `#ffd700`, below cards, visible only when a card is selected. On click: `addCard(selectedCardId, getRun())`, close scene
       - "Skip" button: 16px muted, bottom-right. On click: close scene without adding card
       - If `shouldOfferReward` was false (empty state): show "No Reward" heading, "No cards were offered this time. Continue onward." body, auto-close after 2s or on click

    3. **Create `src/scenes/DeckViewScene.ts`** -- full deck viewer:
       - Overlay panel: centered Rectangle 600x450 at `0x222222`, alpha 0.9
       - Title: "Your Deck" at 24px bold, top-left 24px padding
       - Card count: 14px muted, right of title, e.g. "10 Cards"
       - Card grid: 6 columns, `createCardVisual` at standard size (72x96), 8px gap
       - Grid starts at y=64 within panel
       - If > 18 cards (3 rows), enable vertical scroll via Phaser input drag on the grid container
       - Synergy highlight: cards that form a synergy pair with their adjacent card in deck order get a subtle 2px `0xff00ff` bottom border
       - "Close" button: 16px muted, top-right corner. On click: close scene
       - Reads `getRun().deck.active` for card list

    4. **Create `src/ui/DragDropDeckEditor.ts`** -- draggable card list component:
       - Class `DragDropDeckEditor` with constructor `(scene: Phaser.Scene, x: number, y: number, deckIds: string[], onReorder: (newOrder: string[]) => void)`
       - Renders vertical list of card items, each 580px wide x 48px tall (fits within 640px panel with padding)
       - Card item: type indicator (8px colored circle matching card category), card name (16px white), cooldown (14px muted, right side), cost (14px muted)
       - When drag mode active: show drag handle unicode `:::` (16px muted) at left of each item
       - `setInteractive({ draggable: true })` on each card item
       - On `dragstart`: set depth 999, visually lift card (slight scale up to 1.02)
       - On `drag`: update y position to follow pointer, constrain x to list position
       - On `dragend`: calculate new index based on y position relative to other items, splice the deckIds array, call `onReorder(newOrder)`, rebuild visuals
       - Drop indicator: 4px `0xffd700` line appears between card items at closest drop position during drag
       - `setDragEnabled(enabled: boolean)`: toggle drag mode
       - `destroy()`: cleanup all objects and input listeners
       - Minimum item height 44px (accessibility touch target)

    5. **Create `src/scenes/ShopDeckEditor.ts`** -- shop deck management:
       - Overlay panel: centered Rectangle 640x460 at `0x222222`, alpha 0.9
       - Title: "Reorder Deck" at 24px bold, top-left 24px padding
       - **Reorder mode (inactive):**
         - Display `DragDropDeckEditor` component with drag disabled
         - "Start Reorder (30 Gold)" button: 24px bold, `#ffd700`. On click: if gold >= 30, call `reorderDeck` system to deduct gold, enable drag mode. If gold < 30: show "Not enough gold." flash text (16px `#ff0000`, 800ms)
       - **Reorder mode (active):**
         - Label: "Drag cards to reorder." (16px `#ffd700`)
         - Drag enabled on card list
         - "Done Reordering" button replaces start button. On click: finalize order via `reorderDeck(newOrder, run)`, disable drag mode
       - **Remove functionality:**
         - Each card item has "Remove ({cost} Gold)" button (14px `#ff0000`) at far right
         - On click: show confirmation overlay (secondary panel 300x160 centered):
           - "Remove {cardName}? This costs {cost} gold and cannot be undone." (16px white)
           - "Yes, Remove" button (14px `#ff0000`): call `removeCard(cardId, run)`, refresh list
           - "Keep Card" button (14px muted): close confirmation
         - If gold < cost: show "Not enough gold." flash text
       - Gold display: show current gold at top-right of panel
       - "Close" button at top-right corner

    6. **Rewrite `src/scenes/DeathScene.ts`** -- run statistics on death:
       - Full scene (not overlay), background `0x1a1a2e`
       - Title: "Run Over" at 32px bold, `#ff0000`, centered at y=80
       - Cause: "Defeated by {enemyName}" at 16px muted, centered at y=120
       - Stats panel: Rectangle 400x280 at `0x222222`, alpha 0.85, centered
       - Stats vertical list with 16px gap:
         - "Loops Completed": label 16px muted left, value 24px white right
         - "Total Damage Dealt": label 16px muted left, value 24px white right
         - "Total Cards Played": label 16px muted left, value 24px white right
         - "Total Combos": label 16px muted left, value 24px `#ff00ff` right
       - Values read from: RunState.loop.count for loops, accumulated CombatStats for damage/cards/combos (stored in RunState or passed via scene data)
       - XP warning: "All unbanked XP has been lost." at 16px `#ff0000`, below stats panel with 16px gap
       - "Return to Menu" button: 24px bold `#ffd700`, centered at y=520. On click: `loseAllRunXP(run)`, clear run, transition to MainMenu scene
       - Cleanup on shutdown
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/scenes/PostCombatScene.ts contains `"Battle Summary"` and `"Damage Dealt"` and `"Continue"` and `CombatStats`
    - src/scenes/RewardScene.ts contains `"Choose a Card"` and `generateCardReward` and `addCard` and `"Take Card"` and `"Skip"`
    - src/scenes/DeckViewScene.ts contains `"Your Deck"` and `createCardVisual` and `"Close"` and `getRun().deck.active`
    - src/scenes/ShopDeckEditor.ts contains `"Reorder Deck"` and `DragDropDeckEditor` and `removeCard` and `reorderDeck` and `getRemovalCost`
    - src/scenes/ShopDeckEditor.ts contains `REORDER_SESSION_COST` or `30` as reorder cost
    - src/scenes/DeathScene.ts contains `"Run Over"` and `"Defeated by"` and `"All unbanked XP has been lost."` and `"Return to Menu"` and `loseAllRunXP`
    - src/ui/DragDropDeckEditor.ts contains `export class DragDropDeckEditor` and `setInteractive` and `draggable` and `setDragEnabled`
    - TypeScript compiles with no errors (`npx tsc --noEmit` exits 0)
  </acceptance_criteria>
  <done>All 5 remaining scenes and drag-drop editor component built, all reading from pure TS systems, all using UI-SPEC colors/copy/layout -- compiles clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all combat and deck UI</name>
  <files>src/scenes/CombatScene.ts, src/scenes/PostCombatScene.ts, src/scenes/RewardScene.ts, src/scenes/DeckViewScene.ts, src/scenes/ShopDeckEditor.ts, src/scenes/DeathScene.ts</files>
  <action>
    Human verifies all combat and deck engine UI visually in the browser.
    What was built: Complete combat and deck engine UI -- CombatScene with auto-playing card queue, synergy COMBO! flash, HP/stamina/mana HUD, post-combat summary, card reward choice (pick 1 of 3), deck viewer with synergy highlights, shop deck editor with drag-and-drop reorder and card removal, and death screen with run statistics.
    How to verify:
    1. Run `npx vite dev` and open http://localhost:5173
    2. Start a new run and enter a combat encounter
    3. Verify: cards auto-play from right-side queue, current card pulses with gold glow, cards slide up as played
    4. Verify: HP/Stamina/Mana bars update in real-time at top of screen
    5. Verify: when synergy pair plays, "COMBO!" appears in magenta at center screen
    6. Verify: on victory, "VICTORY" text shows, then post-combat summary with stats (Damage Dealt, Damage Taken, Cards Played, Combos, XP Earned)
    7. Verify: clicking "Continue" shows card reward (3 cards) or auto-continues
    8. Verify: selecting a card highlights it in gold, "Take Card" button appears
    9. Open deck viewer: verify card grid with synergy pair highlights
    10. Open shop deck editor: verify "Start Reorder (30 Gold)" button, drag-and-drop works, removal with confirmation
    11. Let hero die: verify death screen shows "Run Over", cause of death, stats, XP warning, "Return to Menu"
  </action>
  <verify>Human visual inspection -- type "approved" or describe issues to fix</verify>
  <done>All combat UI scenes render correctly, animations play, synergy flashes work, deck editor drag-and-drop functions, death screen shows stats</done>
</task>

</tasks>

<verification>
1. TypeScript compiles clean:
```bash
npx tsc --noEmit
```

2. All unit tests from Plans 01 and 02 still pass:
```bash
npx vitest run --reporter=verbose
```

3. Manual verification of all UI scenes via dev server.
</verification>

<success_criteria>
- Card queue visible during combat with current card highlighted (CMBT-03)
- Synergy triggers show COMBO! flash with visual feedback (CMBT-04)
- Post-combat summary shows all stats (CMBT-05)
- Deck viewer shows full deck order (DECK-07)
- Card reward choice with 3 weighted cards, pick or skip (DECK-08)
- Death screen with run statistics (PLSH-01)
- All scenes compile, all prior tests pass, manual visual verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-combat-deck-engine/02-03-SUMMARY.md`
</output>
