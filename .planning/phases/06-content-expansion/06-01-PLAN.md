---
phase: 06-content-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/types.ts
  - src/data/json/cards.json
  - src/data/json/relics.json
  - src/data/json/enemies.json
  - src/data/json/events.json
  - src/data/json/synergies.json
  - src/data/json/buildings.json
  - tests/content/content.test.ts
autonomous: true
requirements:
  - CONT-05
  - CONT-06
  - CONT-07
  - CONT-08

must_haves:
  truths:
    - "cards.json contains >= 30 cards with valid schema including epic rarity tier"
    - "relics.json contains >= 15 relics with build-around relics at higher shrine tiers"
    - "enemies.json contains >= 5 distinct boss types with bossType and behaviors array"
    - "events.json contains >= 15 events with weight field and material-cost choices"
    - "synergies.json contains >= 10 synergy pairs"
    - "buildings.json forge has maxLevel >= 5 for epic card gating"
    - "buildings.json shrine has maxLevel >= 4 for new relic gating"
    - "All content tests pass with updated assertions"
  artifacts:
    - path: "src/data/types.ts"
      provides: "Epic rarity on CardDefinition, CardUpgrade interface, BossBehavior type, event weight/material types"
    - path: "src/data/json/cards.json"
      provides: "30+ cards with upgraded field and epic tier"
    - path: "src/data/json/relics.json"
      provides: "15+ relics including build-around relics"
    - path: "src/data/json/enemies.json"
      provides: "5+ boss types with behavioral pattern data"
    - path: "src/data/json/events.json"
      provides: "15+ events with weight and material integration"
    - path: "tests/content/content.test.ts"
      provides: "Updated assertions validating Phase 6 content targets"
  key_links:
    - from: "src/data/json/cards.json"
      to: "src/data/types.ts"
      via: "CardDefinition type with epic rarity and upgraded field"
      pattern: "rarity.*epic"
    - from: "src/data/json/enemies.json"
      to: "src/data/types.ts"
      via: "EnemyDefinition with behaviors array"
      pattern: "behaviors"
    - from: "src/data/json/buildings.json"
      to: "src/data/json/cards.json"
      via: "Forge tier 5 unlocks epic cards"
      pattern: "unlockTier.*5"
---

<objective>
Expand all JSON content data to Phase 6 targets and extend type definitions.

Purpose: Populate the game with 30+ cards (including epic tier), 15+ relics (including build-around), 5+ boss types with behavioral patterns, 15+ events with material integration, and 10+ synergy pairs. This is the data foundation that all subsequent system changes operate on.

Output: Updated JSON data files, extended TypeScript types, passing content validation tests.
</objective>

<execution_context>
@C:/Users/nicolas.pereira/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nicolas.pereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-expansion/06-CONTEXT.md
@.planning/phases/06-content-expansion/06-RESEARCH.md

<interfaces>
<!-- Current type definitions the executor MUST extend -->

From src/data/types.ts:
```typescript
export type CardCategory = 'attack' | 'defense' | 'magic';

export interface CardDefinition {
  id: string;
  name: string;
  description: string;
  category: CardCategory;
  effects: CardEffect[];
  cost?: CardCost;
  upgraded?: boolean;           // REPLACE with CardUpgrade object
  upgradeBonus?: { ... };       // REPLACE with upgraded?: CardUpgrade
  cooldown: number;
  targeting: 'single' | 'aoe' | 'lowest-hp' | 'random' | 'self';
  rarity: 'common' | 'uncommon' | 'rare';  // ADD 'epic'
}

export interface EnemyDefinition {
  id: string;
  name: string;
  type: 'normal' | 'elite' | 'boss';
  baseHP: number;
  baseDefense: number;
  attack: EnemyAttack;
  attackCooldown: number;
  goldReward: { min: number; max: number };
  color: number;
  // ADD: bossType?: string; behaviors?: BossBehavior[]; materialReward?: ...
}

export interface EventDefinition {
  id: string;
  title: string;
  description: string;
  choices: EventChoice[];
  // ADD: weight?: number;
}

export type EventChoiceEffect = 'gain_hp' | 'lose_hp' | 'gain_gold' | 'lose_gold' | 'add_card' | 'remove_card' | 'gain_relic' | 'add_curse';
// ADD: 'gain_material' | 'lose_material' | 'upgrade_card'
```

From src/data/json/cards.json (15 existing cards):
- Starter (no unlockSource): strike, defend, heavy-hit, fireball
- Forge tier 1: counter-strike, shield-wall, heal
- Forge tier 2: fury, iron-skin
- Forge tier 3: fortify, arcane-shield
- Forge tier 4: berserker, rejuvenate, mana-drain, weaken

From src/data/json/relics.json (8 existing relics):
- Common (no unlock): bronze_scale, energy_potion, arcane_crystal
- Shrine tier 1: warrior_spirit, iron_will
- Shrine tier 2: berserker_ring
- Shrine tier 3: demon_heart, phoenix_feather

From src/data/json/enemies.json (9 existing, 4 bosses):
- boss_demon (no bossType), boss_tank, boss_berserker, boss_mage

From src/data/json/buildings.json:
- forge: maxLevel 4, 4 tiers
- shrine: maxLevel 3, 3 tiers
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend type definitions and update content tests for Phase 6 targets</name>
  <files>src/data/types.ts, tests/content/content.test.ts</files>
  <read_first>
    - src/data/types.ts
    - tests/content/content.test.ts
    - src/data/json/cards.json
    - src/data/json/relics.json
    - src/data/json/enemies.json
    - src/data/json/events.json
  </read_first>
  <action>
**In src/data/types.ts:**

1. Add `'epic'` to CardDefinition rarity union:
   ```typescript
   rarity: 'common' | 'uncommon' | 'rare' | 'epic';
   ```

2. Replace the existing `upgraded?: boolean` and `upgradeBonus?` fields on CardDefinition with:
   ```typescript
   /** Optional upgrade data overlay -- only changed fields */
   upgraded?: CardUpgrade;
   /** Unlock gating for meta-progression */
   unlockSource?: string;
   unlockTier?: number;
   ```

3. Add CardUpgrade interface (before CardDefinition):
   ```typescript
   export interface CardUpgrade {
     effects?: CardEffect[];
     cost?: CardCost;
     cooldown?: number;
     description?: string;
   }
   ```

4. Add BossBehavior type and extend EnemyDefinition:
   ```typescript
   export type BossBehaviorType = 'enrage' | 'shield' | 'multi_hit' | 'drain' | 'summon';

   export interface BossBehavior {
     type: BossBehaviorType;
     hpThreshold?: number;
     attackSpeedMultiplier?: number;
     interval?: number;
     shieldAmount?: number;
     hitCount?: number;
     damageMultiplier?: number;
     healPercent?: number;
     summonId?: string;
   }
   ```

5. On EnemyDefinition, add optional fields:
   ```typescript
   bossType?: string;
   behaviors?: BossBehavior[];
   materialReward?: {
     chance: number;
     bonusMaterial: string;
     bonusAmount: { min: number; max: number };
   };
   ```

6. Add `'gain_material' | 'lose_material' | 'upgrade_card'` to EventChoiceEffect union type.

7. Extend EventChoiceEffectEntry to support material field:
   ```typescript
   export interface EventChoiceEffectEntry {
     type: EventChoiceEffect;
     value?: number | string;
     material?: string;
   }
   ```

8. Add optional `weight` field to EventDefinition:
   ```typescript
   export interface EventDefinition {
     id: string;
     title: string;
     description: string;
     choices: EventChoice[];
     weight?: number;
   }
   ```

9. Extend EventChoice requirement to support material checks:
   ```typescript
   export interface EventChoice {
     text: string;
     effects: EventChoiceEffectEntry[];
     requirement?: {
       minGold?: number;
       minHP?: number;
       minMaterial?: Record<string, number>;
     };
   }
   ```

**In tests/content/content.test.ts:**

1. Change `relics.json` test from `expect(relics.length).toBe(8)` to `expect(relics.length).toBeGreaterThanOrEqual(15)`.

2. Change `enemies.json` test: update bossTypes assertion to expect 5 boss types instead of 3. Update `bossVariants.length` check to `toBeGreaterThanOrEqual(5)`. Remove the exact `expect(bossTypes).toEqual(...)` and replace with `expect(bossVariants.length).toBeGreaterThanOrEqual(5)`.

3. Change `events.json` count from `toBeGreaterThanOrEqual(5)` to `toBeGreaterThanOrEqual(15)`.

4. Change `cards.json` count from `toBeGreaterThanOrEqual(15)` to `toBeGreaterThanOrEqual(30)`.

5. Add new test in `cards.json` describe: "has at least 3 epic rarity cards":
   ```typescript
   it('has at least 3 epic rarity cards', () => {
     const epics = cards.filter((c: any) => c.rarity === 'epic');
     expect(epics.length).toBeGreaterThanOrEqual(3);
   });
   ```

6. Add test: "each card with upgraded field has valid upgrade object":
   ```typescript
   it('upgraded cards have valid upgrade object with effects or cost or cooldown', () => {
     const upgraded = cards.filter((c: any) => c.upgraded);
     expect(upgraded.length).toBeGreaterThanOrEqual(10);
     for (const card of upgraded) {
       const u = card.upgraded;
       expect(u.effects || u.cost || u.cooldown || u.description).toBeTruthy();
     }
   });
   ```

7. Add test in enemies.json: "boss enemies have behaviors array":
   ```typescript
   it('boss enemies with bossType have behaviors array', () => {
     const bossesWithType = enemies.filter((e: any) => e.bossType);
     for (const boss of bossesWithType) {
       expect(Array.isArray(boss.behaviors)).toBe(true);
       expect(boss.behaviors.length).toBeGreaterThanOrEqual(1);
     }
   });
   ```

8. Add test in events.json: "events with material effects have valid material field":
   ```typescript
   it('at least 3 events reference materials', () => {
     const materialEvents = events.filter((e: any) =>
       e.choices.some((c: any) =>
        c.effects.some((eff: any) => eff.type === 'gain_material' || eff.type === 'lose_material')
       )
     );
     expect(materialEvents.length).toBeGreaterThanOrEqual(3);
   });
   ```

9. Import synergiesData and add synergy count test:
   ```typescript
   import synergiesData from '../../src/data/json/synergies.json';

   describe('synergies.json', () => {
     it('has >= 10 synergy pairs', () => {
       expect((synergiesData as any[]).length).toBeGreaterThanOrEqual(10);
     });
   });
   ```

10. Update buildings.json tests:
    - Change shrine maxLevel test from `toBe(3)` to `toBeGreaterThanOrEqual(4)`
    - Add test: `expect(buildings.forge.maxLevel).toBeGreaterThanOrEqual(5)`
    - Update tier length upper bound from 8 to 10
  </action>
  <verify>
    <automated>cd "C:/Users/nicolas.pereira/OneDrive - Way2 Technology/Área de Trabalho/autoscroller" && npx vitest run tests/content/content.test.ts 2>&1 | tail -5</automated>
    Note: Tests WILL FAIL at this point because JSON data has not been expanded yet. This is expected (RED phase). The tests define the target. Verify that the test file compiles and runs (failures are about count assertions, not syntax errors).
  </verify>
  <acceptance_criteria>
    - src/data/types.ts contains `rarity: 'common' | 'uncommon' | 'rare' | 'epic'`
    - src/data/types.ts contains `export interface CardUpgrade`
    - src/data/types.ts contains `export type BossBehaviorType`
    - src/data/types.ts contains `export interface BossBehavior`
    - src/data/types.ts contains `behaviors?: BossBehavior[]`
    - src/data/types.ts contains `'gain_material' | 'lose_material' | 'upgrade_card'`
    - src/data/types.ts contains `material?: string` in EventChoiceEffectEntry
    - src/data/types.ts contains `weight?: number` in EventDefinition
    - src/data/types.ts contains `minMaterial?: Record<string, number>`
    - tests/content/content.test.ts contains `toBeGreaterThanOrEqual(30)` for cards
    - tests/content/content.test.ts contains `toBeGreaterThanOrEqual(15)` for relics
    - tests/content/content.test.ts contains `toBeGreaterThanOrEqual(15)` for events
    - tests/content/content.test.ts contains `toBeGreaterThanOrEqual(5)` for boss variants
    - tests/content/content.test.ts contains `toBeGreaterThanOrEqual(10)` for synergies
    - tests/content/content.test.ts contains `rarity === 'epic'`
    - tests/content/content.test.ts contains `boss.behaviors`
  </acceptance_criteria>
  <done>Type definitions extended with epic rarity, CardUpgrade, BossBehavior, event material types. Content tests updated with Phase 6 target assertions. Tests compile and run (expected failures on count checks until data expanded).</done>
</task>

<task type="auto">
  <name>Task 2: Expand all JSON content data to Phase 6 targets</name>
  <files>src/data/json/cards.json, src/data/json/relics.json, src/data/json/enemies.json, src/data/json/events.json, src/data/json/synergies.json, src/data/json/buildings.json</files>
  <read_first>
    - src/data/json/cards.json
    - src/data/json/relics.json
    - src/data/json/enemies.json
    - src/data/json/events.json
    - src/data/json/synergies.json
    - src/data/json/buildings.json
    - src/data/json/curses.json
    - src/data/types.ts
    - .planning/phases/06-content-expansion/06-CONTEXT.md
  </read_first>
  <action>
**cards.json -- Add ~15 new cards (total >= 30). Keep all 15 existing cards unchanged. Add `upgraded` field to ALL existing and new cards that are not curses. New cards fill strategic gaps per CONTEXT.md decisions:**

New cards to add (exact stats at Claude's discretion, but follow these archetypes):

**Attack cards (5 new):**
- `cleave` (common): AoE attack, moderate damage to all enemies, stamina cost. Targeting: `aoe`. No unlock.
- `reckless-charge` (uncommon): High single-target damage, loses HP as cost. Forge tier 2.
- `execute` (rare): Massive damage to lowest-hp enemy. High stamina cost. Forge tier 3.
- `chain-lightning` (rare, magic-category but attack-like): Damage + secondary AoE. Mana cost. Forge tier 4.
- `doom-blade` (epic): Very high damage, lose defense AND HP. Forge tier 5. Targeting: single.

**Defense cards (3 new):**
- `parry` (uncommon): Moderate armor + small damage reflect. Stamina cost. Forge tier 1.
- `bulwark` (rare): Large armor gain, reduces move speed (stamina cost). Forge tier 3.
- `last-stand` (epic): Massive armor, only usable below 50% HP (checked by cost system). Mana + stamina cost. Forge tier 5.

**Magic/Utility cards (7 new):**
- `meditate` (common): Generate stamina and mana. No cost. Slow cooldown. No unlock.
- `vampiric-touch` (uncommon): Small damage + heal based on damage dealt. Mana cost. Forge tier 2.
- `haste` (uncommon): Self-buff, next card cooldown halved (debuff type reducing enemy defense as proxy). Mana cost. Forge tier 2.
- `energy-surge` (rare): Large stamina + mana restore. No damage. High cooldown. Forge tier 3.
- `poison-cloud` (rare): AoE debuff damage over time. Mana cost. Forge tier 4.
- `soul-rend` (epic): High damage + heal + mana drain from enemy. Very high mana cost. Forge tier 5.
- `sacrifice` (epic): Lose 20% current HP, deal 3x that as damage to all enemies. No resource cost. Forge tier 5.

Add `upgraded` field to all cards (existing and new). Upgrade pattern:
- Common cards: +25% effect values, OR -1 cost
- Uncommon cards: +30% effect values, OR -1 cooldown reduction (min 0.5s)
- Rare cards: +35% effect values
- Epic cards: +20% effect values (they're already powerful)

Example upgraded field for existing `strike`:
```json
"upgraded": {
  "effects": [{ "type": "damage", "value": 25, "target": "enemy" }],
  "description": "Deal 25 damage."
}
```

**relics.json -- Add 7 new relics (total >= 15). Keep all 8 existing relics unchanged.**

New relics:

**Stat-stick relics (3 new):**
- `vitality_ring` (common, no unlock): +8 Max HP, +4 Max Stamina. passive, stat_bonus. Stats: `{ "maxHP": 8, "maxStamina": 4 }`.
- `mana_stone` (common, no unlock): +6 Max Mana. passive, stat_bonus. Stat: maxMana, value: 6.
- `swift_boots` (rare, shrine tier 2): -10% card cooldowns (effectType: "cooldown_reduction", value: 0.1). trigger: passive.

**Build-around relics (4 new):**
- `thin_deck_charm` (rare, shrine tier 2): "When deck has <= 6 cards, +50% damage". trigger: passive, effectType: "conditional_damage_mult", condition: "deck_size_lte_6", value: 1.5.
- `spell_focus` (rare, shrine tier 3): "Spells cost 0 mana but deal -20% damage". trigger: card_played, effectType: "spell_cost_override", condition: "card_category_magic", stats: { "manaCostOverride": 0, "damageMultiplier": 0.8 }.
- `first_strike_amulet` (epic, shrine tier 3): "First card each combat deals 3x damage". trigger: combat_start, effectType: "first_card_multiplier", value: 3.0, once_per: "combat".
- `blood_pact` (epic, shrine tier 4): "Gain +2 Strength per 10% HP missing". trigger: passive, effectType: "conditional_strength", condition: "hp_missing_per_10pct", value: 2.

**enemies.json -- Add 2 new boss types (total 5+ boss types with bossType). Keep all 9 existing enemies. Update existing bosses to have behaviors array. Add bossType to boss_demon.**

Updates to existing bosses:
- `boss_demon`: Add `"bossType": "demon"`, add `"behaviors": [{ "type": "drain", "healPercent": 50 }]` (already has lifesteal specialEffect, behaviors formalizes it).
- `boss_tank`: Add `"behaviors": [{ "type": "shield", "interval": 10000, "shieldAmount": 60 }]`
- `boss_berserker`: Add `"behaviors": [{ "type": "enrage", "hpThreshold": 0.3, "attackSpeedMultiplier": 2.0 }]`
- `boss_mage`: Add `"behaviors": [{ "type": "summon", "summonId": "mage", "interval": 15000 }]` (Note: summon is data-only for now; AI implementation in Plan 02)

New boss entries:
- `boss_dragon`:
  ```json
  {
    "id": "boss_dragon",
    "name": "Ancient Dragon",
    "type": "boss",
    "bossType": "dragon",
    "baseHP": 1100,
    "baseDefense": 20,
    "attack": { "damage": 28, "pattern": "scaling", "specialEffect": "double" },
    "attackCooldown": 2200,
    "behaviors": [
      { "type": "enrage", "hpThreshold": 0.25, "attackSpeedMultiplier": 1.8 },
      { "type": "multi_hit", "hitCount": 2, "damageMultiplier": 0.6 }
    ],
    "goldReward": { "min": 140, "max": 220 },
    "materialReward": { "chance": 1.0, "bonusMaterial": "essence", "bonusAmount": { "min": 5, "max": 10 } },
    "color": 16744448
  }
  ```

- `boss_hydra`:
  ```json
  {
    "id": "boss_hydra",
    "name": "Swamp Hydra",
    "type": "boss",
    "bossType": "hydra",
    "baseHP": 900,
    "baseDefense": 10,
    "attack": { "damage": 18, "pattern": "random", "specialEffect": "debuff" },
    "attackCooldown": 1800,
    "behaviors": [
      { "type": "multi_hit", "hitCount": 3, "damageMultiplier": 0.45 },
      { "type": "drain", "healPercent": 30 }
    ],
    "goldReward": { "min": 120, "max": 190 },
    "materialReward": { "chance": 1.0, "bonusMaterial": "essence", "bonusAmount": { "min": 4, "max": 8 } },
    "color": 3381555
  }
  ```

**events.json -- Add 10 new events (total >= 15). Keep all 5 existing events. New events use weight field and some reference materials.**

All existing events: add `"weight": 1.0` field.

New events (weight determines frequency -- higher = more common):

1. `wandering_blacksmith` (weight: 1.5): Pay 5 iron for +10 Max HP this run OR pay 3 crystal to upgrade a random card. Material requirement checks.
2. `ancient_library` (weight: 1.0): Pay 20 gold to gain a random card OR spend 3 herbs to heal 30 HP.
3. `dark_ritual` (weight: 0.5): Trade 15 HP for +2 Strength this run (lose_hp + gain_gold proxy) OR gain curse "wound" + gain relic.
4. `merchants_gamble` (weight: 1.5): Pay 50 gold, 50% chance gain rare relic, 50% chance gain curse "fragility".
5. `abandoned_mine` (weight: 1.0): Spend 10 stamina-equivalent (lose_hp 5) to gain 8 iron + 3 crystal. Or leave.
6. `fairy_circle` (weight: 0.8): Gain 15 HP + 5 herbs. Or sacrifice 10 HP to remove a curse.
7. `war_memorial` (weight: 1.0): Gain +30 gold OR gain a random uncommon card. Simple choices.
8. `crystal_cave` (weight: 0.7): Pay 5 iron to gain 10 crystal. Or mine: lose 10 HP, gain 5 iron + 5 stone.
9. `cursed_mirror` (weight: 0.5): Gain a copy of a random card already in your deck + curse "weakness". Or smash mirror: gain 20 gold.
10. `hermits_hut` (weight: 1.2): Free card upgrade (upgrade_card effect) OR heal 25 HP. No cost for either.

Events with material effects must use `gain_material` / `lose_material` types with `material` field:
```json
{ "type": "lose_material", "material": "iron", "value": 5 }
{ "type": "gain_material", "material": "crystal", "value": 10 }
{ "type": "upgrade_card", "value": "random" }
```

Material requirement on choices:
```json
"requirement": { "minMaterial": { "iron": 5 } }
```

**synergies.json -- Add 4+ new synergy pairs (total >= 10). Keep all 6 existing.**

New synergy pairs:
- `cleave` -> `strike`: bonus damage +20 (Momentum!)
- `parry` -> `counter-strike`: bonus damage +30 (Perfect Counter!)
- `meditate` -> `fireball`: bonus damage +25 (Focused Flame!)
- `vampiric-touch` -> `heal`: bonus heal +8 (Life Mastery!)
- `defend` -> `defend`: bonus armor +10 (Fortress!) -- double defend combo

**buildings.json -- Extend forge to maxLevel 6, add tiers 5-6. Extend shrine to maxLevel 4, add tier 4.**

Forge tier 5:
```json
{
  "level": 5,
  "cost": { "iron": 120, "crystal": 80, "essence": 30 },
  "unlocks": { "cards": ["doom-blade", "last-stand", "soul-rend"] },
  "description": "Unlock Doom Blade, Last Stand, and Soul Rend"
}
```

Forge tier 6:
```json
{
  "level": 6,
  "cost": { "iron": 180, "crystal": 120, "essence": 50, "bone": 30 },
  "unlocks": { "cards": ["sacrifice"] },
  "description": "Unlock Sacrifice"
}
```

Update forge `maxLevel` from 4 to 6.

Shrine tier 4:
```json
{
  "level": 4,
  "cost": { "crystal": 80, "essence": 50, "iron": 20 },
  "unlocks": { "relics": ["blood_pact"] },
  "description": "Unlock Blood Pact relic"
}
```

Update shrine `maxLevel` from 3 to 4.
  </action>
  <verify>
    <automated>cd "C:/Users/nicolas.pereira/OneDrive - Way2 Technology/Área de Trabalho/autoscroller" && npx vitest run tests/content/content.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - cards.json `.cards` array has length >= 30
    - cards.json contains card with `"rarity": "epic"`
    - cards.json contains card with `"id": "doom-blade"`
    - cards.json contains card with `"id": "cleave"` and `"targeting": "aoe"`
    - cards.json `strike` entry contains `"upgraded"` object with effects array
    - relics.json array has length >= 15
    - relics.json contains `"id": "thin_deck_charm"`
    - relics.json contains `"id": "blood_pact"` with `"unlockSource": "shrine"` and `"unlockTier": 4`
    - enemies.json contains `"id": "boss_dragon"` with `"bossType": "dragon"` and `"behaviors"` array
    - enemies.json contains `"id": "boss_hydra"` with `"bossType": "hydra"`
    - enemies.json `boss_demon` has `"bossType": "demon"` field
    - enemies.json `boss_tank` has `"behaviors"` array
    - events.json array has length >= 15
    - events.json contains `"id": "wandering_blacksmith"` with `"weight"` field
    - events.json contains at least one choice with `"type": "gain_material"`
    - events.json contains at least one choice with `"type": "lose_material"`
    - events.json contains at least one choice with `"type": "upgrade_card"`
    - synergies.json array has length >= 10
    - buildings.json `forge.maxLevel` is >= 5
    - buildings.json `shrine.maxLevel` is >= 4
    - buildings.json forge has tier with level 5
    - `npx vitest run tests/content/content.test.ts` exits 0 (all tests pass)
  </acceptance_criteria>
  <done>All JSON content expanded to Phase 6 targets. 30+ cards with epic tier and upgrade data, 15+ relics with build-around types, 5+ boss types with behavioral patterns, 15+ events with material integration, 10+ synergy pairs. Content tests all pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/content/content.test.ts --reporter=verbose` -- all assertions pass
- `npx vitest run --reporter=verbose` -- full suite green (no regressions from type changes)
- cards.json has >= 30 entries with valid schema
- All epic cards have unlockSource "forge" and unlockTier >= 5
- All new relics with unlock have unlockSource "shrine"
- All boss enemies have bossType and behaviors array
</verification>

<success_criteria>
- Content test suite passes with all Phase 6 target assertions
- Full vitest suite passes (no regressions)
- JSON data is well-formed and follows existing schemas
- Type definitions compile without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-expansion/06-01-SUMMARY.md`
</output>
